import { ApiGroup } from '../ApiGroup';
import { SetResponseType } from '../types';
export declare class Host extends ApiGroup {
    info(dateTime: Date, platform: string, userAgent: string, version: string): Promise<SetResponseType>;
}
