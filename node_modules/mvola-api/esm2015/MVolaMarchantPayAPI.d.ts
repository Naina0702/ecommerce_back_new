import { Transaction } from "./Transaction";
import { AuthResult } from "./@types";
export declare class MVolaMarchantPayAPI extends Transaction {
    private baseURL;
    constructor(url?: string);
    revokeToken(consumerKey: string, consumerSecret: string, updateToken?: boolean): Promise<AuthResult>;
    setAccessToken(accessToken: string): void;
}
